/**
 * Two-state enum of elements which toggles hidden content.
 * Such as accordions, dropdowns, tooltips, etc.
 * @readonly
 * @enum {string}
 */
export const DROPDOWN_STATES = {
  opened: `opened`,
  closed: `closed`
}
